// 0 va voir le fichier db.json crée un heros dans la base de donnée
// 1 puis commencer ici (allez du modele a l'interface) 
export class Hero {
    constructor(public name: string, public id?:number) {}; // ? = optionnel
}
